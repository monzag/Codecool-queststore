<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>questore</title>
    <link href="/static/assets/css/main.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Muli" rel="stylesheet">
</head>

<body>
<header class="primary-header">
    <img src="/static/assets/pics/web/logo.jpg">
    <nav class="primary-nav">
        <ul>
            <li><a href="">about</a></li><!--
     --><li><a href="">contact</a></li><!--
     --><li><a class="logout-btn" href="">logout</a></li>
        </ul>
    </nav>
</header>
<section class="menu-bar">
    <div class="frame">
        <img src="/static/assets/pics/profiles/student.jpg">
        <nav>
            <ul class="menu-nav">
                <li><a href="/mentor">profile</a></li>
                <li><a href="/mentor/groups">groups</a></li>
                <li><a href="/mentor/quest/show">quests</a></li>
                <li><a href="/mentor/artifact/show">artifacts</a></li>
            </ul>
        </nav>
    </div>
    <footer>
        <small>&copy; pyLamas</small>
    </footer>
</section>

<form id="2" class="add" method="POST">
    {% if artifact != null %}
        <h3>Edit artifact {{ artifact.getName }}</h3>
    {% else %}
        <h3>Add new artifact</h3>
    {% endif %}

    {% if artifactName.length > 0 %}
        <div class="fail">
            <h5>Artifact with this name already exists!</h5>
        </div>
    {% endif %}

    <table class="input-table">
        <tr>
            <th>Artifact Name:</th>
            <th>
                <input type="text" name="artifactName"
                       value="{{ artifact.getName }}{{ artifactName }}"
                       title="Name should be between 3 and 17 letters long"
                       pattern="[a-zA-ZąćęłńóśźżĄĆĘŁŃÓŚŹŻ ]{3,17}"
                       required="required">
            </th>
        </tr>
        <tr>
            <th>Description:</th>
            <th>
                <input type="text" name="description"
                       value="{{ artifact.getDescription }}{{ description }}"
                       title="Only letters and spaces, at last 3"
                       pattern="[a-zA-ZąćęłńóśźżĄĆĘŁŃÓŚŹŻ ]{3,}"
                       required="required">
            </th>

        </tr>
        <tr>
            <th>Price:</th>
            <th>
                <input type="text" name="price"
                       value="{{ artifact.getPrice }}{{ price }}"
                       title="Only numbers, and don't be to harsh 1000000 is to mutch"
                       pattern="[1-9][0-9]{1,5}"
                       required="required">
            </th>
        </tr>
        <tr>
            <th>Type:</th>
            <th>
                <select name="type">
                    <option value="Single">Single</option>
                    <option value="Team">Team</option>
                </select>
            </th>
        </tr>
    </table>
    <button class="submit" type="submit" form="2">submit</button>
</form>

</body>
</html>
