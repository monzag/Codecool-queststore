<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>questore</title>
    <link href="/static/assets/css/main.css" rel="stylesheet" type="text/css">
    <link href="https://fonts.googleapis.com/css?family=Muli" rel="stylesheet">
</head>

<body>

<header class="primary-header">
    <img src="/static/assets/pics/web/logo.jpg">
    <nav class="primary-nav">
        <ul>
            <li><a href="https://codecool.pl/" target="_blank">about</a></li>
            <li><a href="">contact</a></li>
            <li><a class="logout-btn" href="">logout</a></li>
        </ul>
    </nav>
</header>

<section class="menu-bar">
    <div class="frame">
        <img src="/static/assets/pics/profiles/student1.jpg">
        <nav>
            <ul class="menu-nav">
                <li><a href="/student">profile</a></li>
                <li><a href="/student/wallet">wallet</a></li>
                <li><a href="/student/store">store</a></li>
                <li><a href="/student/team_purchases">team purchases</a></li>
            </ul>
        </nav>
    </div>

    <footer>
        <small>&copy; pyLamas</small>
    </footer>
</section>
<form class="add" method="POST">
    {% if errmsg.length > 0 %}
    <div class="fail">
        <h5>{{ errmsg }}!</h5>
    </div>
    {% endif %}
    <h3>Choose students to buy {{ artifact.getName() }}</h3>

    <table class="input-table">
        <tr>
            <th>Student #1:</th>
            <th>
                <select name="student">
                    {% for student in students %}
                    <option value="{{ student.getLogin().getValue() }}">{{ student.getName() }}{{ student.getSurname() }}</option>
                    {% endfor %}
                </select>
            </th>
        </tr>
        <!--<tr>-->
            <!--<th>Student #2:</th>-->
            <!--<th>-->
                <!--<select name="student">-->
                    <!--{% for student in students %}-->
                    <!--<option value="{{ student.getLogin() }}">{{ student.getName() }}{{ student.getSurname() }}</option>-->
                    <!--{% endfor %}-->
                <!--</select>-->
            <!--</th>-->
        <!--</tr>-->
        <!--<tr>-->
            <!--<th>Student #3:</th>-->
            <!--<th>-->
                <!--<select name="student">-->
                    <!--{% for student in students %}-->
                    <!--<option value="{{ student.getLogin() }}">{{ student.getName() }}{{ student.getSurname() }}</option>-->
                    <!--{% endfor %}-->
                <!--</select>-->
            <!--</th>-->
        <!--</tr>-->
        <!--<tr>-->
            <!--<th>Student #4:</th>-->
            <!--<th>-->
                <!--<select name="student">-->
                    <!--{% for student in students %}-->
                    <!--<option value="{{ student.getLogin() }}">{{ student.getName() }}{{ student.getSurname() }}</option>-->
                    <!--{% endfor %}-->
                <!--</select>-->
            <!--</th>-->
        <!--</tr>-->
        <tr>
            <th><button class="submit" type="submit">Finish</button></th>
            <th class="btn" id="remove-btn"><a method="GET" href="/student/store">Cancel</a></th>
        </tr>
    </table>
</form>
</body>
</html>